1. 如果您的操作系统是Windows7，需要使用Vb6_SP6_Fix_for_Win7_and_Upper.exe覆盖安装完成的VB6 SP6执行文件，
否则在界面上拖动控件会一跳一跳的闪烁

2. 如果是Windows10，还需要执行VB6_AppCompat(patcher for win10).zip里面的install.cmd，否则在界面上选择多个控件时不显示选择框

3. Windows10建议使用管理员权限和兼容模式运行VB6，感觉速度快一些

1. For Windows7, you need to use Vb6_SP6_Fix_for_Win7_and_Upper.exe to overwrite the installed VB6 SP6 executable file,
Otherwise, dragging the controls on the VB6 GUI will flicker.

2. For Windows 10, you also need to execute install.cmd in VB6 AppCompat(patcher for win10).zip, 
otherwise you cannot select multiple controls.

3. recommends running VB6 in Windows10 with administrator privileges and compatibility mode, it is faster.

